# Index

Explaination of the content of each directory.

* `cube` contains the target-selected scripts you'll want to use
* `shconf` contains functions utility functions used by shconf
* `makeable` contains script to make associated targets